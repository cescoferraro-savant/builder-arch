import slowNode from "./images/slow_node.png";
import scope from "./images/slow_scope.png";

# Reasoning

- Why is it slow?
  - business logic is intertwined with ui logic
  - components are too “smart”
  - file scafolding is not intuitive or inexistent

---

# Why is it slow?

- components are too complex & files are too big

<div
  style={{
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  }}
>
  <img src={slowNode} style={{ maxWidth: 600 }} alt="sdfk" />
</div>

---

# Why is it slow?

- hook share scope

<div
  style={{
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  }}
>
  <img src={scope} style={{ maxWidth: 600 }} alt="sdfk" />
</div>
